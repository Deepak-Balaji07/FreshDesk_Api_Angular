 FreshDesk API – Angular

This project demonstrates how to integrate the Freshdesk API using Angular to fetch and manage support ticket data. It showcases making authenticated HTTP requests, handling API responses, and dynamically displaying ticket information using Angular services and components. :contentReference[oaicite:1]{index=1}

🚀 Features
- Fetch and display Freshdesk tickets using REST API
- Handle API responses with proper error handling
- Use Angular services to separate API logic
- Dynamic UI rendering with Angular components

 🧠 Technologies Used
- Angular (latest version)
- TypeScript
- HTML & CSS
- Freshdesk REST API :contentReference[oaicite:2]{index=2}

 🔧 Setup & Installation
1. Clone the repository:

   git clone https://github.com/Deepak-Balaji07/FreshDesk_Api_Angular.git
   
2.  Install dependencies:

     npm install

    Add your Freshdesk API key and domain into the appropriate service file.

3. Run the development server:

   ng serve

4.  Open http://localhost:4200/ in your browser.
